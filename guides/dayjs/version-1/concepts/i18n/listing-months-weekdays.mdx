---
slug: listing-months-weekdays
title: Listing the months and weekdays of the current locale
label: Listing the months and weekdays of the current locale
visibility: PUBLIC
---

To get the list of months or weekdays in a locale.

<Callout type="info">
This requires the `LocaleData` plugin to work.
</Callout>

```js
dayjs.extend(localeData)

dayjs.weekdays()
dayjs.weekdaysShort()
dayjs.weekdaysMin()
dayjs.monthsShort()
dayjs.months() // e.g. return [ 'January','February','March','April','May',
// 'June','July','August','September','October','November','December' ]
```